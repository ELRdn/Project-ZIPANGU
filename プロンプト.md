# Project ZIPANGU — K/C/Z Multi-Class Architecture Migration & RX 7600 Local Research Phase

Project ZIPANGU の研究計画を、従来の単一モデル `ZIPANGU-C-I-9B` 中心設計から、

**K / C / Z の3階級モデルファミリー**

へ正式に拡張してください。

今回の変更は単なるモデル追加ではなく、

* モデル命名
* research roadmap
* training pipeline
* dataset recipe
* evaluation
* local hardware validation
* quantization
* variant policy
* future QAT research

まで含むProject ZIPANGU全体の設計変更です。

既存実装を必ず読んだ上で、互換性を壊さず段階的に移行してください。

---

# 0. 最重要方針

Project ZIPANGUの新しい研究戦略は、

```text
K
↓
C
↓
Z
```

の段階的scale-upとする。

意味：

```text
K = Kei / 軽量級
C = Chū / 中量級
Z = Zyū / 重量級
```

Generationは従来どおり、

```text
I → RO → HA → NI → ...
```

というIROHA順。

初代はすべてGeneration I。

---

# 1. 正式な初代モデルファミリー

## ZIPANGU-K-I-4B

Canonical base:

```text
empero-ai/Qwen3.8-4B-Distill
```

役割：

* Project ZIPANGU最初の実験機
* dataset recipe validation
* LoRA / QLoRA pipeline validation
* Japanese adaptation effect validation
* local AMD GPU研究
* cheap scaling experiment
* Cへの移行判断用

Kの目的は必ずしもLLM-jp-4-8Bを直接倒すことではない。

Kでは、

```text
Vanilla
↓
ZIPANGU adaptation
↓
Japanese performance improvement
+
general reasoning retention
```

が再現可能に確認できれば成功。

---

## ZIPANGU-C-I-9B

Canonical base:

```text
empero-ai/Qwen3.8-9B-Distill
```

役割：

**Project ZIPANGUの第一本命モデル。**

主要目標：

```text
LLM-jp-4-8B Thinking
```

を複数の未汚染日本語evaluationで上回る。

従来のProject ZIPANGU C-I研究計画はここへ継承する。

ただし、

**K-Iで有効性を確認したrecipeをCへscaleする**

ことを原則とする。

いきなりCをblindに本番trainingしない。

---

## ZIPANGU-Z-I-26B-A4B

Canonical base:

```text
google/gemma-4-26B-A4B-it
```

役割：

* heavyweight class
* K/Cで確立したJapanese adaptation recipeのscale-up
* LLM-jp-4 32B-A3B級との比較
* MoE post-training研究
* quantization-aware downstream research
* 将来的なクラウドGPU / ABCI / funding対象

Zは現在のimmediate training targetではない。

設計とconfigを準備するが、

**今回学習は開始しない。**

---

# 2. CanonicalモデルとHeretic/Abliterated variantを分離する

以下のcommunity modified modelsは研究上重要だが、

**ZIPANGUのcanonical baseにはしない。**

参考候補：

```text
petruhonk/Qwen3.8-9B-Distill-uncensored-heretic

insraq/Qwen3.5-4B-EmperoAI-Qwen3.8-Distill-Heretic-Abliterated

TrevorJS/gemma-4-26B-A4B-it-uncensored
```

理由：

ZIPANGUが検証したい主要因は、

```text
strong clean base model
+
Japanese curated post-training
```

の効果。

Heretic / Abliteration / refusal-removal済みmodelをbaseにすると、

```text
Japanese adaptation effect
+
weight surgery effect
```

が混ざり、研究上の因果が不明確になる。

---

# 3. Heretic variant policy

Canonical modelの学習・評価完了後、

必要であれば別variantとしてHeretic処理を行う。

命名案：

```text
ZIPANGU-K-I-4B
ZIPANGU-K-I-4B-Heretic

ZIPANGU-C-I-9B
ZIPANGU-C-I-9B-Heretic

ZIPANGU-Z-I-26B-A4B
ZIPANGU-Z-I-26B-A4B-Heretic
```

重要：

CanonicalとHereticは別artifact。

Heretic版をcanonical releaseへ上書きしない。

推奨順序：

```text
clean base
↓
ZIPANGU Japanese post-training
↓
merge
↓
canonical evaluation
↓
optional Heretic / Abliteration
↓
same evaluation again
```

理由：

先にHeretic処理をしてからSFTすると、

trainingによってrefusal behaviorを再学習する可能性がある。

したがって原則、

**Japanese training → Heretic**

の順を研究候補とする。

---

# 4. Community Heretic policy

Project ZIPANGU本家は、

```text
Japanese adaptation
dataset curation
benchmarking
reproducibility
```

を中心責任範囲とする。

Heretic / Abliteration / uncensored variantについては、

将来的に第三者community forkへ任せることも正式な選択肢とする。

README等では、

```text
Official ZIPANGU
Community Variants
```

を明確に区別できる設計にする。

---

# 5. Research Roadmapを正式変更

旧：

```text
C-I-9B
↓
LLM-jp-4-8B
↓
Gemma 26B
```

新：

```text
Phase K
ZIPANGU-K-I-4B
↓
recipe validation
↓
local AMD experimentation
↓
1M
↓
10M
↓
optional 50M

Phase C
ZIPANGU-C-I-9B
↓
Kで勝ったrecipeをscale
↓
LLM-jp-4-8B Thinking本戦

Phase Z
ZIPANGU-Z-I-26B-A4B
↓
K/C recipeをMoEへscale
↓
QAT研究
↓
LLM-jp-4-32B-A3B級比較
```

この方針を、

* README
* RESEARCH_PLAN
* MODEL_NAMING
* configs
* dashboard
* report template

へ反映する。

---

# 6. Dataset pipelineをclass-independentへ一般化

既存pipelineに、

`c-i`

という固定文字列やC-I固有pathが多く存在する場合、

K/C/Z共通化を進める。

理想：

```text
configs/models/
  k-i-4b.yaml
  c-i-9b.yaml
  z-i-26b-a4b.yaml

configs/datasets/
  i-jp-heavy.yaml
  i-balanced.yaml

configs/train/
  k-i/
  c-i/
  z-i/
```

Generation Iのdataset philosophyは基本共有できる。

つまり、

```text
Generation I recipe
```

と

```text
Model Class
```

を分離する。

---

# 7. 推奨モデルconfig schema

例：

```yaml
id: ZIPANGU-K-I-4B

class: K
generation: I

base_model:
  repo: empero-ai/Qwen3.8-4B-Distill
  revision: null

architecture:
  family: qwen3.5
  type: dense
  parameters: 4B

research_role:
  - recipe_validation
  - local_training
  - amd_gpu_validation

training:
  enabled: false
  preferred_method: qlora

evaluation:
  compare_base: true
  compare_llm_jp: optional
```

C:

```yaml
id: ZIPANGU-C-I-9B

base_model:
  repo: empero-ai/Qwen3.8-9B-Distill

research_role:
  - primary_japanese_model
  - llm_jp_4_8b_challenge
```

Z:

```yaml
id: ZIPANGU-Z-I-26B-A4B

base_model:
  repo: google/gemma-4-26B-A4B-it

architecture:
  type: moe
  total_parameters: 26B
  active_parameters: 4B

research_role:
  - heavyweight
  - moe_adaptation
  - qat_research
```

---

# 8. K-Iを新しいimmediate targetへ変更

現在のOvernight Finalizationや1M Pilot関連コードが、

`empero-ai/Qwen3.8-9B-Distill`

をhardcodeしている場合、

**immediate pilot targetをK-I-4Bへ切り替える。**

新しい初回pilot：

```text
ZIPANGU-K-I-4B
Base:
empero-ai/Qwen3.8-4B-Distill
```

ただし、

C-I-9B用existing configは削除しない。

Kが成功した後に再利用する。

---

# 9. K-I success criteria

K-Iでは、

LLM-jpを倒すことを絶対条件にしない。

Primary comparison:

```text
empero-ai/Qwen3.8-4B-Distill Vanilla
vs
ZIPANGU-K-I-4B
```

見るもの：

* Japanese MT-Bench
* llm-jp-instructions
* AnswerCarefully
* Japanese math
* Japanese writing
* general reasoning
* English/general regression

Success：

### Required

日本語primary metricsの複数で明確な改善。

### Required

general reasoningに重大な崩壊がない。

### Required

training/eval contaminationなし。

### Desired

LLM-jp-4-8Bへ一部benchmarkで接近または勝利。

KがLLM-jpを直接超えた場合はbonus result。

---

# 10. RX 7600 Local Research Phaseを追加

ユーザーPC：

```text
GPU:
AMD Radeon RX 7600
8GB VRAM

CPU:
Ryzen 9 7900X

RAM:
96GB
```

RX 7600を、

**ZIPANGU-Kのlocal research GPU**

として活用したい。

目的：

1. llama.cpp Vulkan inference
2. llama.cpp ROCm/HIP inference
3. ROCm 10系の実験
4. PyTorch ROCm確認
5. Unsloth AMD確認
6. K-I 4B QLoRA smoke可能性
7. quantized ZIPANGU inference benchmark

---

# 11. llama.cpp Vulkan backend test

Vulkan backendを用意。

可能なら公式/upstream llama.cppを使用。

build example：

```text
GGML_VULKAN=ON
```

ただし実際のcurrent build systemを確認して使用。

テストmodelは、

Qwen3.8-4B系のGGUFがローカル/公式communityに存在する場合利用。

無ければ勝手に不明なweightを使わず、

model acquisition planを報告。

---

# 12. llama.cpp ROCm / HIP backend test

RX 7600 = `gfx1102` を明示的に確認。

ROCm/HIP buildで、

```text
RX 7600
```

を正しく選択できるか診断。

Ryzen 9 7900X側iGPUが列挙される可能性がある。

絶対に、

**iGPU benchmarkをRX7600 benchmarkとして記録しない。**

device:

* PCI ID
* gfx arch
* VRAM
* backend device name

をmanifestへ保存。

---

# 13. ROCm 10 Experimental Track

ユーザーはROCm 10系を試したい。

ROCm 10については、

**experimental track**

として扱う。

既存の安定したROCm環境を破壊しない。

原則：

```text
stable environment
+
experimental ROCm 10 environment
```

を分離する。

可能なら、

* separate venv
* separate WSL distro
* separate build directory
* separate Docker/ROCm environment

等を使う。

既存working environmentへin-place upgradeしない。

---

# 14. Vulkan vs ROCm benchmark

同一model・同一GGUF・同一prompt・同一context条件で比較。

最低限：

```text
CPU
Vulkan
ROCm/HIP
```

結果：

```text
prompt processing tok/s
generation tok/s
time to first token
VRAM
RAM
GPU utilization
stability
errors
backend version
```

を記録。

複数回実行してwarmupの影響を分離。

最低：

```text
warmup x1
measurement x3
```

推奨。

---

# 15. benchmark matrix

少なくとも：

```text
Model:
Qwen3.8-4B

Quantization:
Q4_K_M or nearest appropriate GGUF
optional Q5/Q6

Context:
2K
4K
8K
```

のうち現実的な範囲。

8GB VRAMを超える設定を無理に実行しない。

OOM時は安全停止。

---

# 16. RX7600 device-selection safety

以下をmanifest化：

```text
selected_gpu
available_devices
backend
gfx_arch
VRAM
driver
ROCm version
Vulkan driver
llama.cpp commit
```

Ryzen iGPUが見つかった場合：

```text
device_role: iGPU
exclude_from_primary_benchmark: true
```

とする。

---

# 17. PyTorch ROCm diagnostic

現在Unsloth環境がCPU判定だった原因を診断する。

確認：

```python
import torch

torch.__version__
torch.version.hip
torch.cuda.is_available()
torch.cuda.device_count()
torch.cuda.get_device_name(...)
```

ROCm PyTorchではCUDA-compatible APIを使う場合があるため、

`torch.cuda.*`

という名前だけでCUDA GPUだと誤解しない。

保存：

```text
reports/hardware/rocm_pytorch_diagnostic.json
```

---

# 18. Unsloth AMD diagnostic

Unslothが現在：

```text
CPU environment
```

として認識された理由を調査。

確認：

* installed torch build
* ROCm-enabled torchか
* Unsloth version
* backend selection
* device enumeration
* RX7600 recognition
* iGPU mis-selection
* Triton/backend dependency
* bitsandbytes dependency
* flash-attention / linear attention compatibility

重要：

**診断段階で既存Unsloth Studio環境を破壊しない。**

---

# 19. K-I 4B local QLoRA smoke

Unsloth + ROCmが正常に動く場合のみ、

極小training smokeを試してよい。

ただし今回も、

**本番1M trainingは自動開始しない。**

Smoke：

```text
samples: 50–200
sequence: 512 or 1024
steps: 5–20
LoRA rank: small
checkpoint: temporary
```

見るもの：

* model load
* forward
* backward
* loss
* optimizer step
* VRAM peak
* checkpoint save
* adapter reload
* inference

Successしたら：

```text
RX7600_UNSLOTH_SMOKE=PASS
```

---

# 20. 8GB VRAM safety

RX7600は8GB。

したがって、

model load前にVRAM estimateを行う。

OOM retry loopは禁止。

試す順：

```text
4bit
small sequence
batch 1
gradient accumulation
gradient checkpointing
small LoRA rank
```

必要ならCPU/RAM offload可能性を検討するが、

極端に遅くなる場合は、

```text
LOCAL_TRAINING_NOT_PRACTICAL
```

と結論してよい。

無理やり通すことが目的ではない。

---

# 21. RX7600の役割

RX7600を、

```text
production training GPU
```

として定義しない。

正式な役割：

```text
Local Research / CI GPU
```

用途：

* format smoke
* LoRA smoke
* tokenizer
* GGUF inference
* Vulkan benchmark
* ROCm benchmark
* quantization validation
* adapter sanity checks

本格trainingは将来的にRunPod等へscale可能。

---

# 22. K-I training scale policy

正式scale：

```text
Smoke
↓
1M
↓
evaluation
↓
10M
↓
evaluation
↓
optional 50M
```

各scaleでHuman GO必須。

1Mが改善しなければ、

10Mへ自動scaleしない。

---

# 23. K → C recipe transfer

Kで成功した場合、

以下をfreeze：

```text
dataset sources
dataset token ratio
quality thresholds
dedup policy
contamination policy
chat template
LoRA target strategy
LR
sequence length
assistant loss policy
```

Cへscaleする際、

一度に複数要因を変更しない。

K→Cでモデルサイズ以外を可能な限り固定し、

**scaling effect**

を観察できるようにする。

---

# 24. C-I本番方針

C:

```text
empero-ai/Qwen3.8-9B-Distill
```

は、

Kで有効性を証明したrecipeのみ利用。

Primary opponent:

```text
llm-jp/llm-jp-4-8b-thinking
```

勝利条件：

既存Project ZIPANGUルールを継承：

* primary Japanese eval 3つ中2勝以上
* general regression gate PASS
* contamination PASS
* baseline比較あり
* raw outputs保存

---

# 25. Z-I Gemma clean base

Zのcanonical base：

```text
google/gemma-4-26B-A4B-it
```

Heretic版ではない。

ZではGemma 4 MoE特性を明示的に扱う。

---

# 26. Gemma 4 QAT research track

将来研究用として、

Google公式QAT系checkpointを別trackで扱える設計を準備する。

候補例：

```text
google/gemma-4-26B-A4B-it-qat-q4_0-unquantized
```

このcheckpointは、

QAT pipeline後のhalf precision/unquantized weightとして扱う。

ただし、

**通常SFT後にQAT特性が完全維持されるとは仮定しない。**

---

# 27. Z QAT A/B research design

将来：

## Route A

```text
normal Gemma BF16
↓
ZIPANGU training
↓
merge
↓
Q4 quantization
```

## Route B

```text
Google QAT-unquantized checkpoint
↓
ZIPANGU training
↓
merge
↓
Q4
```

## Route C

```text
Gemma
↓
ZIPANGU training
↓
QAT-aware finalization
↓
Q4
```

比較：

```text
BF16 score
Q4 score
BF16 → Q4 degradation
Japanese benchmark
general benchmark
```

これはZ世代の正式研究候補としてdocsへ追加。

---

# 28. Quantization artifact naming

将来：

```text
ZIPANGU-K-I-4B
ZIPANGU-K-I-4B-Q4_K_M

ZIPANGU-C-I-9B
ZIPANGU-C-I-9B-Q4_K_M

ZIPANGU-Z-I-26B-A4B
ZIPANGU-Z-I-26B-A4B-Q4_0
```

などをartifact metadataで管理。

Model family名とquantization名を混同しない。

---

# 29. llama.cpp deployment validation

Kのadapter merge後、

将来的に：

```text
HF weights
↓
GGUF conversion
↓
Q4_K_M
↓
llama.cpp
↓
RX7600 Vulkan
↓
RX7600 ROCm
```

をProject ZIPANGU release validation pipelineの一部にする。

Kクラスは、

**8GB consumer AMD GPUで実用可能**

を重要な価値の一つとして評価したい。

---

# 30. Hardware reports

新規：

```text
reports/hardware/
```

を作成。

最低限：

```text
RX7600_ENVIRONMENT.md
LLAMACPP_VULKAN.md
LLAMACPP_ROCM.md
VULKAN_VS_ROCM.md
UNSLOTH_AMD.md
```

machine-readable：

```text
reports/hardware/results.json
```

---

# 31. Hardware CLI

可能なら：

```bash
python scripts/hardware_probe.py
```

で、

* CPU
* RAM
* GPU
* VRAM
* ROCm
* HIP
* PyTorch
* Vulkan
* llama.cpp
* Unsloth

を一覧化。

さらに：

```bash
python scripts/benchmark_local_inference.py
```

でbackend比較。

---

# 32. Stable vs Experimental environment separation

ROCm 10などの実験で、

Project ZIPANGU本体環境を壊さない。

推奨構造：

```text
.envs/
  stable/
  rocm10-experimental/
```

またはREADMEで明確に別環境として管理。

actual virtualenv binariesはGitに入れない。

environment lock / manifestのみ保存。

---

# 33. Baseline strategy変更

従来：

```text
C-I 9B Vanilla
```

だけがbaseline targetだった。

今後：

```text
K-I Base Vanilla
K-I ZIPANGU

C-I Base Vanilla
C-I ZIPANGU

Z-I Base Vanilla
Z-I ZIPANGU
```

をclassごとに比較。

つまりevaluation pipelineも、

`model_id`

parameterを取るgeneric設計へ変更。

---

# 34. Existing pretrain_finalization generalization

現在：

```text
scripts/pretrain_finalization.py
```

にC-I固有hardcodeがある場合、

generic化する。

例：

```bash
python scripts/pretrain_finalization.py \
  --model ZIPANGU-K-I-4B \
  --stage all
```

将来：

```bash
--model ZIPANGU-C-I-9B
--model ZIPANGU-Z-I-26B-A4B
```

で使えるようにする。

---

# 35. Dataset outputs

従来：

```text
data/processed/c-i/
```

なら、

```text
data/processed/generation-i/
```

または、

```text
data/processed/i/
```

の共通dataset candidateと、

class別training-format outputを分離することを検討。

理由：

KとCで同じGeneration I recipeを比較できるから。

ただしmodel tokenizer差があるため、

**tokenized/materialized training formatはmodel別**

にする。

ZはGemma tokenizerなので明確に別。

---

# 36. Token accounting

K/CはQwen系だが、

同じTokenizerとは決めつけずrevisionを固定。

ZではGemma tokenizerで必ず再tokenize。

Dataset recipeの比率は、

**semantic category target**

として共有。

actual token massはモデルごとに計算する。

---

# 37. Project README更新

README冒頭でモデルfamilyを見せる。

例：

```text
Project ZIPANGU

K — Lightweight
ZIPANGU-K-I-4B
Base: Qwen3.8-4B-Distill
Status: First research target

C — Middleweight
ZIPANGU-C-I-9B
Base: Qwen3.8-9B-Distill
Status: Primary LLM-jp challenge

Z — Heavyweight
ZIPANGU-Z-I-26B-A4B
Base: Gemma 4 26B-A4B
Status: Future scale-up
```

---

# 38. Model family diagram

README/docsへ：

```text
Generation I
              ┌─ K-I-4B
Recipe I ─────┼─ C-I-9B
              └─ Z-I-26B-A4B
```

を入れる。

目的：

**model sizeとtraining recipe generationを分離して見せる。**

---

# 39. Research philosophy追加

Project ZIPANGUのテーマを明文化：

> Can careful low-cost Japanese adaptation of strong open-weight models compete with Japanese-focused scratch-trained foundation models?

さらに：

> Validate cheaply at K, scale to C, and only then move to Z.

を研究原則とする。

---

# 40. Cost philosophy

Project ZIPANGUは、

**GPUを使う前にローカルで可能な検証を最大限行う。**

優先：

```text
RX7600
↓
Community Cloud 4090
↓
H100
↓
large-scale Z training
```

いきなり高額GPUへ行かない。

---

# 41. RunPod policy更新

Default pilot GPU：

Kの場合：

```text
RX7600 local first
```

local不可能または非実用の場合：

```text
RTX4090 Community
```

C：

```text
RTX4090 Community
```

Z：

後日別途決定。

RunPod budget safetyは既存ルールを継承。

---

# 42. Human approval gate

今回の変更を実装しても、

```text
TRAINING_ALLOWED=false
```

を維持。

以下を勝手に実行しない：

* K 1M training
* C training
* Z training
* RunPod creation
* Heretic processing
* model upload
* Hugging Face publish
* GitHub push
* destructive cleanup

---

# 43. 今回実際に実行してよいこと

ローカル・無料で安全なら実施可：

### Software

* config migration
* docs update
* tests
* pipeline generalization

### RX7600

* hardware detection
* Vulkan detection
* ROCm detection
* PyTorch ROCm diagnostic
* llama.cpp build
* llama.cpp benchmark
* Unsloth diagnostic

### Small smoke

条件が整えば、

K-I 4Bの**極小QLoRA smoke**のみ実行可能。

ただし事前に：

* no paid compute
* no destructive environment change
* no large dataset processing

を確認。

---

# 44. No blind environment destruction

ROCm 10 testingで、

現在のworking Python / Unsloth / ComfyUI等を壊さない。

禁止：

```text
global pip uninstall
global torch replacement
system ROCm overwrite
working venv destruction
```

実験はisolated environmentで行う。

---

# 45. Tests

最低限追加：

* model registry supports K/C/Z
* generation recipe separation
* K default target
* C remains configured
* Z remains training-disabled
* Heretic cannot become canonical accidentally
* model-specific tokenizer accounting
* generic finalization CLI
* local GPU probe
* device-selection logic
* iGPU exclusion
* benchmark manifest
* training remains blocked

既存40 testsを壊さない。

---

# 46. Completion reports

以下を作る。

```text
reports/migration/KCZ_MIGRATION_REPORT.md

reports/hardware/
  RX7600_ENVIRONMENT.md
  LLAMACPP_VULKAN.md
  LLAMACPP_ROCM.md
  VULKAN_VS_ROCM.md
  UNSLOTH_AMD.md
```

---

# 47. Final status format

終了時：

## KCZ MIGRATION

```text
K model config
C model config
Z model config
Generation-I recipe
Heretic variant policy
```

## RX7600

```text
Vulkan
ROCm/HIP
ROCm10 experimental
PyTorch ROCm
Unsloth
```

をそれぞれ：

```text
PASS
PARTIAL
BLOCKED
NOT_TESTED
```

で表示。

## BENCHMARK

もし実行できたら：

```text
backend
model
quant
context
pp tok/s
tg tok/s
VRAM
RAM
stability
```

## LOCAL TRAINING

```text
K-I 4B QLoRA smoke:
PASS / BLOCKED
```

## NEXT STEP

最後に最大5項目のみ。

---

# 48. 最終的なProject ZIPANGUロードマップ

今回の実装後、READMEと研究計画が以下と一致していることを確認。

```text
Project ZIPANGU
│
├── Generation I
│
│   ├── ZIPANGU-K-I-4B
│   │   └── Qwen3.8-4B-Distill
│   │
│   ├── ZIPANGU-C-I-9B
│   │   └── Qwen3.8-9B-Distill
│   │
│   └── ZIPANGU-Z-I-26B-A4B
│       └── Gemma 4 26B-A4B
│
├── Optional Community Variants
│   ├── K-I-Heretic
│   ├── C-I-Heretic
│   └── Z-I-Heretic
│
└── Release Artifacts
    ├── HF
    ├── LoRA
    ├── GGUF
    └── Quantized variants
```

---

# 49. 今回の優先順位

最優先：

1. K/C/Z設計へmigration
2. K-Iをimmediate target化
3. C-I existing workを保持
4. Z-I future config追加
5. Heretic policy分離
6. RX7600 Vulkan test
7. RX7600 ROCm/HIP test
8. ROCm10 experimental validation
9. Unsloth + ROCm diagnosis
10. 可能ならK-I 4B QLoRA smoke

---

# 50. 完了条件

理想：

```text
[PASS] K/C/Z model architecture migration
[PASS] K-I immediate target
[PASS] C-I preserved
[PASS] Z-I future config
[PASS] canonical vs Heretic separation
[PASS] RX7600 Vulkan
[PASS/PARTIAL] RX7600 ROCm
[PASS/PARTIAL] ROCm10
[PASS/PARTIAL] Unsloth AMD
[PASS/PARTIAL] K-I 4B local QLoRA smoke
[BLOCKED intentionally] 1M real training
[BLOCKED intentionally] RunPod
[BLOCKED intentionally] C/Z training
```

質問で止まらず、安全かつ無料でローカル実行できるところまで進めてください。

ただし、

**環境を壊してまでPASSさせないこと。**

Project ZIPANGUにとって重要なのは、

```text
reproducibility
scientific separation
low cost
safe scaling
```

です。

最後にチャットでは、

```text
KCZ MIGRATION RESULT
RX7600 RESULT
VULKAN VS ROCM
UNSLOTH STATUS
K-I LOCAL TRAINING STATUS
FILES CHANGED
BLOCKERS
NEXT EXACT STEP
```

の8項目だけで簡潔に報告してください。
